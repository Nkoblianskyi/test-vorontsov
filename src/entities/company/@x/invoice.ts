/**
 * Cross-import API (Feature-Sliced "@x" notation): the invoice entity prints the
 * seller from the company profile. The company never depends on invoices.
 */
export type { CompanyProfile } from "../model/schema";
